server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: password
    username: user
    url: jdbc:postgresql://localhost:5440/postgres
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    show-sql: true
  #   database-platform: orj.hibernate.
